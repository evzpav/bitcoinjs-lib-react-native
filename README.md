# bitcoinjs-lib on React Native

First steps to make [bitcoinjs-lib](https://github.com/bitcoinjs/bitcoinjs-lib) work on React Native without Expo.

## Install dependencies
```bash
  npm install
```

## Run
```bash
  npm start
```